<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>
    
    <style name="main_btn_style">
        <item name="android:background">@color/main_btn_bg</item>
        <item name="android:layout_gravity">center</item>
        <item name="android:layout_width">@dimen/activity_main_btn_width</item>
        <item name="android:layout_margin">@dimen/activity_main_btn_margin</item>
    </style>

</resources>
